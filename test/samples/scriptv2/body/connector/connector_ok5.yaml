body:
  - connector:  
      type: "RemoteShell"
      arguments: 
        remoteOS: "GNU"
        host: "{$CURRENT_IP}"
        username: "usuario"
        password: "usuario"
        port: !!int 22

  - run:
      connector: "RemoteShell"
      command: "echo {$CURRENT_IP}"
      expected: "localhost"