folder: "C:\\Users\\fher\\source\\repos\\AutoCheck\\test\\samples\\scriptv2\\body\\run\\"

body:  
  - run:
      connector: "LOCALSHELL"           
      command: "CheckIfFileExists"
      arguments:  
        path: "{$CURRENT_FOLDER}"
        file: "{$CURRENT_FILE}"
        recursive: !!bool False

  - run:
      connector: "LOCALSHELL"           
      command: "CheckIfFileExists"
      arguments:  
        path: "{$CURRENT_FOLDER}"
        file: "{$CURRENT_FILE}"
        recursive: !!bool False
      expected: ""

  - run:
      connector: "LOCALSHELL"           
      command: "GetFile"
      arguments:  
        path: "{$CURRENT_FOLDER}"
        file: "{$CURRENT_FILE}"
        recursive: !!bool False
      expected: "%run_ok2.yaml"