body:     
  - run:
      command: "echo {$SCRIPT_NAME}"
      expected: "vars_ok1"

  - run:
      command: "echo {$CURRENT_FILE}"
      expected: "vars_ok1.yaml"

  - run:
      command: "echo {$RESULT}"
      expected: "vars_ok1.yaml"

  - run:
      command: "echo {$CURRENT_FOLDER}"
      expected: "%vars"

  - run:
      command: "echo {$NOW}"
      expected: "!="